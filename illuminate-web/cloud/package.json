{
  "name": "illuminate-cloud-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Illuminate Cloud — Teams: a data-driven multi-repo workspace dashboard that renders only live /api/workspace data, built to a single self-contained dist/index.html.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
